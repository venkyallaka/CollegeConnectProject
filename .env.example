# Example environment variables for CollegeConnect
PORT=8000
# Path inside the container to the CSV (if mounted or copied)
DATA_PATH=/app/college_students_30000.csv
# Add any API keys or secrets here (do NOT commit real secrets)
# OPENAI_API_KEY=
